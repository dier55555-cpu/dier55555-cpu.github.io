{
  "_comment": "Положить как mcp.json в этой папке кабинета (mcp.json в .gitignore). URL — рабочий MCP Нои; ключ только в Secrets.",
  "mcpServers": {
    "noya": {
      "url": "PASTE_NOYA_MCP_URL",
      "headers": {
        "Authorization": "Bearer ${env:NOYA_KEY_DENDRIIT}"
      }
    }
  }
}
