{
  "_comment": "Скопируйте в mcp.json (в .gitignore). URL — как в рабочем MCP Нои; ключ только в Secrets как NOYA_KEY_DENDRIIT.",
  "mcpServers": {
    "noya": {
      "url": "PASTE_NOYA_MCP_URL",
      "headers": {
        "Authorization": "Bearer ${env:NOYA_KEY_DENDRIIT}"
      }
    }
  }
}
