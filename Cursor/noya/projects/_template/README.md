# Проект Нои: `<slug>`

Заполнить и переименовать папку `_template` → `<slug>`.

| Поле | Значение |
|------|----------|
| Кабинет | `<имя>` (`<uuid>`) |
| Доска | `<имя>` (`<uuid>`) |
| Секрет Cursor | `NOYA_KEY_<SLUG>` |
| Агенты (править) | … |
| Агенты (не трогать) | … |

Перед работой: `instruction_guide` → `whoami` → сверить с `.cursor/rules/noya-cabinets.mdc`.

Общие правила кабинетов и ключей: `Cursor/noya/_shared/cabinets-and-keys.md`.
