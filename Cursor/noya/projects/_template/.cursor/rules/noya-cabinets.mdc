---
description: Кабинет и агенты Нои в этой папке. Не копировать ключи.
alwaysApply: true
---

# Кабинеты Нои (эта папка)

Кабинет: `<имя>` (`<uuid>`)
Доска: `<имя>` (`<uuid>`)

Агенты:
- `<имя>` (`<uuid>`) — правим
- `<имя>` (`<uuid>`) — не править без прямой команды

Секрет Cursor: `NOYA_KEY_<SLUG>` (значение только в Secrets, не сюда).

Методология: `Cursor/noya/_shared/Методология_НОЯ.txt`
Ключи и мультипрофиль: `Cursor/noya/_shared/cabinets-and-keys.md`
Ключи API в git и в этот файл не писать.
