---
description: Кабинет Нои этой папки. Не копировать ключи.
alwaysApply: true
---

# Кабинет Нои: `<имя>`

Кабинет: `<имя>` (`<uuid>`)
Доска: `<имя>` (`<uuid>`)
Секрет Cursor: `NOYA_KEY_<CABINET>`

## Проекты этого кабинета

| Папка | Проект | Заметки |
|-------|--------|---------|
| `<project>/` | … | … |

## Агенты

- `<имя>` (`<uuid>`) — правим
- `<имя>` (`<uuid>`) — не править без прямой команды

Ключи API в git и в этот файл не писать.
