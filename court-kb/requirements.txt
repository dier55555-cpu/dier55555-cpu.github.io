requests>=2.31
beautifulsoup4>=4.12
trafilatura>=1.8
PyYAML>=6.0
tldextract>=5.1

# Для mcp_server/ (демо MCP-инструмента "слой 2" — живой поиск по базе знаний).
# Версия закреплена: в mcp>=2.0 класс FastMCP был переработан/переименован,
# при обновлении сверяйтесь с актуальным API MCP Python SDK.
mcp==1.12.4

# Слой 2 — поиск конкретного дела: решение капчи (опционально, только если
# на форме суда есть капча и вы платите за 2captcha/rucaptcha).
2captcha-python>=1.2

# Тесты
pytest>=8.0

# Справочник судов России (дамп DaData → Excel/JSON)
openpyxl>=3.1

# HTTP-обёртка (api/) для интеграции через вебхук из n8n/НОЕ
fastapi>=0.110
uvicorn[standard]>=0.29
