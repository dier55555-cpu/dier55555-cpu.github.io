# Ассистент-юриста — карточка агента для НОЕ

Кабинет **Студия ДендрИИт**. Одна доска на двоих. Агента «Легион — продавец (тест)» не править.

| Поле | Значение |
| --- | --- |
| Кабинет | Студия ДендрИИт (`508ba66a-64d6-4aae-942b-f0d023222e9a`) |
| Доска | **Основной** (`ab67516b-90e3-45f6-9a96-1f5174089f08`) — общая с Легионом |
| Имя агента | Ассистент - юриста (тест) (`d0b7f22e-b80a-403f-864a-cdf12c2dbebb`) |
| Роль | Справка по 6 райсудам Воронежа + помощь найти карточку дела |
| Канал | **тестовый чат НОЕ** |
| Дожим / продажа | Выключены у этого агента (у Легиона — свои, не трогаем) |
| База знаний | Коллекция «Суды Воронежа» (`c36e77a3-bff0-4bd2-be6a-f85d52b418fc`), привязана только к юристу; документы — `kb/` |
| Инструкция | `prompt.md`, залита через API (prompt_version 2) |
| Инструмент | «Поиск дела в суде» (`06e7d3ef-d8ce-4ab8-acc5-baec246cb86b`), action `http` → n8n-вебхук, привязан только к юристу |
| n8n | процесс «Суды Воронежа — вебхук юриста» на `n8n-app.noya-ai.ru`, webhook `court-agent-yurist`, Active |
| Проверка | `test-dialogs.md` |

Порядок заполнения агента (для истории/повтора) — `ASSEMBLE.md`.

## Статус на 19.08.2026

- Инструкция и БЗ залиты через API Нои (аккаунтный ключ `nk_live_...`), Легион не тронут.
- Создан отдельный n8n-процесс «Суды Воронежа — вебхук юриста» (Webhook POST `/webhook/court-agent-yurist`), активен. Пока отвечает заглушкой `{"status":"disabled", ...}` — реального парсера/RU-прокси на сервере ещё нет.
- В Ное создан инструмент **«Поиск дела в суде»** (параметры: `court_slug` enum из 6 судов, `case_number`, `last_name`; действие `http` → тот же вебхук) и привязан **только** к агенту «Юрист».
- Сквозной тест (`POST /api/agents/:id/test`) подтвердил: агент вызывает инструмент, получает `disabled`, честно отвечает и даёт ссылку на сайт суда — карточку дела не выдумывает.
- На доске уже был пустой инструмент «Новое действие» (создан вручную до этой сессии, URL пустой; тоже привязан к юристу) — не трогали, решите сами: удалить или донастроить.
- Следующий шаг: поднять `court-kb` (API + сам парсер, папка выше по дереву репозитория) на сервере с российским IP и переключить n8n-вебхук с заглушки на реальный вызов `/case-lookup`.
